// Re-export from the new product catalog for backward compatibility.
// All new code should import from '@/data/product-catalog' directly.
export { productData, colourways } from './product-catalog';
